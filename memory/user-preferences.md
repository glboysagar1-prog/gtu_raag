# User Preferences

### Models: Default Embedding Model
- **Observed:** this session
- **Context:** User explicitly requested to ALWAYS use BGE-large embedding model.
- **Detail:** When doing RAG, vector retrieval, or anything involving embeddings, default to `BAAI/bge-large-en-v1.5` or `bge-large` depending on the integration.
